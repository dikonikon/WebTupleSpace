# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# webtuplespace

POST        /webtuplespace/write                                    controllers.WebTupleSpace.write

POST        /webtuplespace/read                                     controllers.WebTupleSpace.read

GET         /webtuplespace/start                                    controllers.WebTupleSpace.startSession

POST        /webtuplespace/subscribe/session/*id                    controllers.WebTupleSpace.subscribe(id)

GET         /webtuplespace/notifications/session/*id                controllers.WebTupleSpace.notifications(id)

GET         /webtuplespace/notificationhistory/session/*id          controllers.WebTupleSpace.notificationHistory(id)

GET         /webtuplespace/notificationsreceived/session/*id        controllers.WebTupleSpace.notificationsReceived(id)

POST        /webtuplespace/take                                     controllers.WebTupleSpace.take

# Home page
GET         /                                                       controllers.Application.index

GET         /test                                                   controllers.Application.test

GET         /webtuplespace/reset                                    controllers.Application.reset

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                                           controllers.Assets.at(path="/public", file)
